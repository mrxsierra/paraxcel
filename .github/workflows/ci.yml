name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.12.1'  # Adjust to your Python version

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run tests
      run: |
        pytest tests --cov=src

    - name: Security check
      run: |
        pip install bandit
        bandit -r src

    - name: Build executable
      run: |
        pip install pyinstaller
        pyinstaller --onefile src/ui/interface.py  # Adjust the entry point if necessary
